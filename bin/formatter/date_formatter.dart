import 'formatter.dart';

/// 日期格式化
class DateFormatter extends Formatter<DateTime> {
  @override
  String format(DateTime? value) {
    // TODO: implement format
    throw UnimplementedError();
  }
}
